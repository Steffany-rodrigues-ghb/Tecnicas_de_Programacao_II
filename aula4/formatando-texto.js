const nomes = ["ana","bruno","carla"];
const formatados_maiusculo = nomes.map(nome => nome.toUpperCase());
const formatados_minusculo = nomes.map(nome => nome.toLowerCase());

console.log(formatados_maiusculo);
console.log(formatados_minusculo);




